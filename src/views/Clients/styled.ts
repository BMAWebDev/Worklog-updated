import styled from 'styled-components';
// components
import Flex from '@components/Flex';

const AddClientModal = styled(Flex)`
  width: 100vw;
  height: 100vh;
`;

const Container = styled(Flex)`
  max-width: 500px;
  width: 100%;
`;

const Style = { AddClientModal, Container };

export default Style;
